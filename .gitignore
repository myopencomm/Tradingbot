# Secrets & données personnelles — NE JAMAIS COMMITTER
.env
*.log
*.log.*

# Portfolio personnel (utiliser positions.example.json comme template)
# Ce fichier contient vos positions et données financières

# Portfolio personnel (utilisez positions.example.json comme template)
positions.json
trades_history.json
CLAUDE_TRADING_CONTEXT.md
CLAUDE.md
macro_analysis.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/

# Environnement virtuel
venv/
env/
.venv/

# IDE
.vscode/
.idea/
*.swp

# Claude Code local settings (permissions, etc.)
.claude/

# Mode Playwright — état + captures d'exploration (données perso)
bot_state.json
.playwright-mcp/
bd_*.png
*.png
PLAYWRIGHT_STRATEGY.md

# Cache sentiment social (données dérivées, régénéré à chaque run)
sentiment_cache.json
# Cache du résumé macro (dérivé de macro_analysis.md, données perso)
macro_summary_cache.json
# Suivi des coûts API (conso perso)
api_costs.json
dashboard-link.local.txt

# Univers de marché découvert automatiquement (régénérable, volumineux)
universe_cache.json
