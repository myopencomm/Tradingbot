# Core
requests>=2.31.0
python-dotenv>=1.0.0
schedule>=1.2.0
pytz>=2024.1
yfinance>=0.2.40

# AI providers — installez seulement celui que vous utilisez
anthropic>=0.40.0
openai>=1.0.0                    # optionnel
mistralai>=1.0.0                 # optionnel
groq>=0.9.0                      # optionnel — tier GRATUIT disponible
google-generativeai>=0.8.0       # optionnel — tier GRATUIT disponible
Pillow>=10.0.0                   # requis pour Gemini vision

# Recherche web gratuite (pas de clé API — requests pur, pas de lib externe)
# duckduckgo-search obsolète, remplacé par scraping HTML direct

# Import CSV portefeuille
pandas>=2.0.0
vaderSentiment

# Graphiques chandeliers pour analyse technique vision
mplfinance>=0.12.0

# Mode Playwright — pilotage réel de Bourse Direct (connexion, ordres, sync).
# Après installation : venv/bin/playwright install chromium
# (Était absent de ce fichier jusqu'au 11/08/2026 alors que TOUTE l'intégration
# BD en dépend : une installation neuve suivant le README échouait.)
playwright>=1.40.0

# Tests — ./bot.sh test
pytest>=8.0.0
